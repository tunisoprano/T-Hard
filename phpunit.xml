<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <!--
            ÖNEMLİ — <server> blokları neden var (kozmetik değil):

            Docker'da docker-compose, DB_CONNECTION=pgsql / DB_DATABASE=t_hard /
            QUEUE_CONNECTION=rabbitmq değerlerini GERÇEK ortam değişkeni olarak
            veriyor. PHPUnit'in <env> elemanları force="true" ile bile sadece
            putenv() ve $_ENV'i güncelliyor, $_SERVER'a DOKUNMUYOR. Laravel'in
            env() helper'ı ise $_SERVER'ı okuduğu için container'ın değerleri
            kazanıyordu: testler SQLite yerine GERÇEK Postgres'e bağlanıp
            RefreshDatabase ile veritabanını migrate:fresh yapıyor, yani tüm
            veriyi siliyordu. (Bu bilfiil yaşandı, veri kaybedildi.)

            Aşağıdaki <server> elemanları $_SERVER'ı da test değerlerine
            sabitliyor; <env force="true"> ise getenv()/$_ENV tarafını
            kapatıyor. İkisi birlikte, ortam değişkeni olsun ya da olmasın
            testlerin daima izole SQLite + sync kuyrukla çalışmasını garanti
            ediyor.
        -->
        <env name="APP_ENV" value="testing" force="true"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file" force="true"/>
        <env name="BCRYPT_ROUNDS" value="4" force="true"/>
        <env name="BROADCAST_CONNECTION" value="null" force="true"/>
        <env name="CACHE_STORE" value="array" force="true"/>
        <env name="DB_CONNECTION" value="sqlite" force="true"/>
        <env name="DB_DATABASE" value=":memory:" force="true"/>
        <env name="DB_URL" value="" force="true"/>
        <env name="MAIL_MAILER" value="array" force="true"/>
        <env name="QUEUE_CONNECTION" value="sync" force="true"/>
        <env name="SESSION_DRIVER" value="array" force="true"/>
        <env name="PULSE_ENABLED" value="false" force="true"/>
        <env name="TELESCOPE_ENABLED" value="false" force="true"/>
        <env name="NIGHTWATCH_ENABLED" value="false" force="true"/>

        <server name="APP_ENV" value="testing"/>
        <server name="APP_MAINTENANCE_DRIVER" value="file"/>
        <server name="BCRYPT_ROUNDS" value="4"/>
        <server name="BROADCAST_CONNECTION" value="null"/>
        <server name="CACHE_STORE" value="array"/>
        <server name="DB_CONNECTION" value="sqlite"/>
        <server name="DB_DATABASE" value=":memory:"/>
        <server name="DB_URL" value=""/>
        <server name="MAIL_MAILER" value="array"/>
        <server name="QUEUE_CONNECTION" value="sync"/>
        <server name="SESSION_DRIVER" value="array"/>
        <server name="PULSE_ENABLED" value="false"/>
        <server name="TELESCOPE_ENABLED" value="false"/>
        <server name="NIGHTWATCH_ENABLED" value="false"/>
    </php>
</phpunit>
